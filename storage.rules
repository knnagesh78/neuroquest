rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // This version stores text notes in Firestore. File uploads are not enabled.
    // Add explicit per-user paths and file validation before building attachments.
    match /{allPaths=**} { allow read, write: if false; }
  }
}
